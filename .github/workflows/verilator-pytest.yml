name: Verilator Pytest

on:
  push:
  pull_request:

jobs:
  verilator-pytest:
    runs-on: ubuntu-latest
    env:
      TOOLCHAIN_ROOT: /usr
      RISCV_PREFIX: riscv64-unknown-elf-
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y verilator gcc-riscv64-unknown-elf python3-pytest

      - name: Build Verilator model
        run: make verilate

      - name: Run pytest
        run: pytest -v
